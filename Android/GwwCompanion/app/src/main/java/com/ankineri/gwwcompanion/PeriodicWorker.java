package com.ankineri.gwwcompanion;

/*
public class PeriodicWorker  {
    public static void schedule(Context context) {
        PeriodicWorkRequest wr = new PeriodicWorkRequest.Builder(PeriodicWorker.class, 1, TimeUnit.MINUTES).build();
        WorkManager.getInstance(context).enqueue(wr);
    }
    public PeriodicWorker(@NonNull Context context, @NonNull WorkerParameters workerParams) {
        super(context, workerParams);
    }

    @NonNull
    @Override
    public Result doWork() {
        Log.d("GWW", "In worker!");
        return Result.success();
    }
}
*/